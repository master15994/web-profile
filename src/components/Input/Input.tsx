import React from 'react';
import PropTypes from 'prop-types';

export const Input = ({ primary, size, label, border }) => {
    return (
        <input
            type="text" />
    )
}




export default Input